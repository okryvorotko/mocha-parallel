{
  "name": "github-actions",
  "version": "1.0.0",
  "scripts": {
    "test:parallel": "mocha -g @todo --parallel --jobs 2 -r test/base-parallel.test.js",
    "test:single": "mocha -g @todo"
  },
  "dependencies": {
    "mocha": "^10.0.0"
  }
}
